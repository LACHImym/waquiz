-- ============================================================
--  マイグレーション：questions に「解答解説」列を追加
--  ------------------------------------------------------------
--  すでにテーブルを作成済みの場合、このSQLを Supabase の
--  SQL Editor で1回だけ実行してください（既存データは保持されます）。
--  ※ これから schema.sql で新規作成する人は不要です（最新版に含まれています）。
-- ============================================================
alter table questions add column if not exists explanation text default '';
