-- ============================================================
--  外部からの読み取りを減らす（第1段階・今すぐ実行できます）
--  ------------------------------------------------------------
--  アプリが読む必要のないテーブルの「読み取り許可」を外します。
--  アプリの動作に影響が出るのは、下の1点だけです：
--    ・オーナー画面の「照合（申告 vs 解答記録）」表示が使えなくなります
--      → 代わりに、このファイルの末尾のSQLで確認してください。
-- ============================================================
-- ===== ここから =====

-- ------------------------------------------------------------
-- 1) wao_answers（誰がどの問題を正解したか）を読めなくする
--    大会は終了しており、アプリからの書き込みももうありません。
--    これで「誰が何を間違えたか」が外部から見えなくなります。
-- ------------------------------------------------------------
drop policy if exists "read wao_answers" on wao_answers;

-- ------------------------------------------------------------
-- 2) history（作問・編集の履歴）を読めなくする
--    画面から編集履歴を撤去した時点で、アプリは読んでいません。
--    書き込みは続くので、記録自体は残ります。
-- ------------------------------------------------------------
drop policy if exists "read history" on history;

-- ===== ここまで =====


-- ============================================================
--  【次の大会のための備え】
--  ------------------------------------------------------------
--  今回、大会の開催中もランキング（wao_entries）が外部から
--  読める状態でした。次回は下のポリシーに入れ替えると、
--  締切を過ぎるまで誰も順位を見られなくなります。
--  日付は次の大会の締切に合わせて書き換えてください。
--  ※ いま実行すると今回の順位も見えなくなる（アーカイブが
--    空になる）ので、次回の直前に実行してください。
-- ============================================================
-- drop policy if exists "read wao_entries" on wao_entries;
-- create policy "read wao_entries" on wao_entries for select
--   using (now() >= timestamptz '2027-01-01 00:00+09');


-- ============================================================
--  オーナー用：申告された正解数と解答記録の照合
--  （アプリの画面のかわりに、ここで確認してください）
-- ============================================================
-- select e.user_name as "名前",
--        e.correct as "申告した正解数",
--        count(a.*) filter (where a.is_correct) as "記録上の正解数",
--        e.answered as "申告した回答数",
--        count(a.*) as "記録上の回答数",
--        case when e.correct = count(a.*) filter (where a.is_correct)
--              and e.answered = count(a.*) then 'OK' else '⚠ 要確認' end as "照合"
--   from wao_entries e
--   left join wao_answers a on a.user_handle = e.user_handle
--  group by e.user_handle, e.user_name, e.correct, e.answered
--  order by "照合" desc, e.correct desc;
